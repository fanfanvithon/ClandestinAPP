import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RegistrarRepartidorPage } from './registrar-repartidor.page';

describe('RegistrarRepartidorPage', () => {
  let component: RegistrarRepartidorPage;
  let fixture: ComponentFixture<RegistrarRepartidorPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RegistrarRepartidorPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });







  
});
